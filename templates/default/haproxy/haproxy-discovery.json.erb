{
    "name": "haproxy-discovery",
    "commons" : {
        "ec2-tagging" : {
          <% if node['commons'] and node['commons']['ec2-tagging'] and node['commons']['ec2-tagging']['query_tags'] -%>
            "query_tags" : <%= node['commons']['ec2-tagging']['query_tags'].to_json %>
          <% end -%>
        }
    },
    "run_list": ["alfresco::haproxy-config"]
}
